(function($){
    $(window).load(function() {
        //set height Content and Sidebar
        /**
        var main_content = $('#main-content'),
            sidebar = $('#sidebar');
        if($(window).width() > 991 ) {
            if( main_content.height() > sidebar.height() ) {
                sidebar.height(main_content.height());
            } else {
                main_content.height(sidebar.height());
            }
        }
         */
    });
}(jQuery));